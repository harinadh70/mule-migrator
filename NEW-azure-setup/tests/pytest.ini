[pytest]
testpaths = .
asyncio_mode = auto
markers =
    unit: Unit tests (no external dependencies)
    integration: Integration tests (require mocked DB)
    e2e: End-to-end tests (full pipeline)
    slow: Slow tests (LLM calls, large payloads)
